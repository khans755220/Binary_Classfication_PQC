To run the Binary Classification code, first open the random_gen.py file.

In the random_gen.py file, for the variables Class_A and Class_B, insert X and Y coordinates depending on what characteristics
the dataset needs.

Next copy the values for Class_A and Class_B, and insert it into the BC_Simulator.py, under the same variable names.

Run BC_Simulator
